class Solution {
    public double myPow(double x, int n) {
        // double a = Math.abs(n);
        // double squr =Math.abs( Math.pow(x,a));
        // System.out.println(squr);
        // if(squr==0.0) return 0;
        // if(n<0) return (1.0/squr);
       return Math.pow(x,n);
    }
}